#include<stdio.h>
// String is an array of characters .Data of same type are stored in an array.

// There is no string data type in c programming 
// We Declare string type as declearing a one dimensional array.

// Eg: char string_name[string_size];

int main() {
    char str[]="Hello Their";
    printf("%s", str);
    return 0;
}